---
  - name: Install kubernetes related packages
    yum:
      name: "{{ packages }}"
      state: present
    vars:
      packages:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - kubeadm
      - kubelet
      - kubectl